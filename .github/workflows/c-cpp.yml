name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: compile sources
      run: |
        test -n "$(find src -name "*.cpp" -print -quit)"
        find src -name "*.cpp" -print0 | xargs -0 g++ -std=c++17 -c
